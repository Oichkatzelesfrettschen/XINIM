#include <cstdlib>

/**
 * @file t10a.cpp
 * @brief Minimal test that exits immediately with success.
 */

/**
 * @brief Entry point for the t10a test.
 *
 * Simply returns 0 so the caller knows the test succeeded.
 */
int main() {
    return 0; // Indicate success to the caller.
}
